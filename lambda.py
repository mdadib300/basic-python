# Lambda is a shortcut function which is for one-time-use or it is called throw away function. This function has no name, it is stored in a variable. 
# The syntax is: variableName = lambda parameter : expression 
add = lambda x, y : x+y
print(add(5, 7))